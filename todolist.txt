WETH  WBTC

1. 主页 各种池子 收益情况

    a. 池子列表
    b。收益情况，池子内token的数量；
    d。币种价格；
    e。登录

2. 池子的详情页面 （定投， 赎回，再平衡）

    a. 池子状态，偏移量（待定），创建人 owner；
    b。投资收益历史趋势（折线图）；
    c。定投；    （ 每次定投，给投资人 +10 RBT ）
    d。取回token；
    e。再平衡； （ 给操作人 +20RBT，给平台抽出0.5%收益 ）
    g。池子最近10条投资记录；
    h。当前用户投了多钱token；

3. 创建池子

    a。指定 WBTC  WETH 的比例；  （消耗： -20 RBT）
    b。偏移量；
    c。定时触发再平衡；（用户可以选择 是 按照定时处理，还是超过偏移量 再触发）




合约部分：

    1. 合约创建 池子（比例，是否按照时间触发平衡，偏移量）；
    2. 获取池子列表（token名字，以及对应的比例，创建人，偏移量）；
    3. 定投（发交易）；
    4. 取回token（全部取回）；
    5. reblance
        调用 uniswap 进行token转换；
    6. 当前用户的token数量；

    7. ERC20代币 RBT（10w 个）；


中心化服务器：

    1. 获取币价 api；
    2. 投资收益历史趋势；
    3. 定时监听合约是否超过偏移量；
